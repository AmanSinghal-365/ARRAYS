#include<iostream>
using namespace std;

int max(int a[],int size)
{
    int maximum=a[0];
    for(int i=0;i<size;i++)
    {
        if(a[i]>maximum)
        maximum=a[i];
    }
    return maximum;
}

int main()
{
    int a[]={2,4,1,6,8,9,0};
    int size=7;
    int maximum=max(a,size);
    cout<<"MAXIMUM NUMBER IS : "<<maximum<<endl;
}

INT_MIN AND INT_MAX ARE LEAST AND LARGEST VALUES OF INT...
